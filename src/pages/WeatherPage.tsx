import { h } from 'preact';

export const WeatherPage = () => {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h1>Weather Page</h1>
      <p>Weather information will be displayed here.</p>
      {/* You can add a link back or other navigation if needed */}
    </div>
  );
}; 